# 📂 مخطط الحزم والمسارات الجديدة

يوضح هذا الملف تنظيم المشروع بعد إعادة التجميع في حزم بسيطة تحت `apps/`. استخدمه كمرجع سريع لمعرفة ما يجب رفعه على الـVPS، وما يعمل محليًا، وما يخص الواجهة والتطبيق.

## نظرة سريعة
- **apps/cloud-laravel** – باك اند + واجهة Laravel جاهزة للنشر على الـVPS (Nginx/PHP-FPM) مع بيانات تجريبية.
- **apps/edge-server** – خادم FastAPI المحلي لتشغيل الـAI، معالجة الفيديو، التكاملات، والتخزين المحلي (يبنى كـ EXE أيضًا).
- **apps/mobile-app** – تطبيق Flutter (Android/iOS) للوصول الميداني والإشعارات.
- **docs/** – كل الأدلة التشغيلية والتكامل.
- **scripts/** – أدوات مثل سكربت التغليف `scripts/package.sh` لإنتاج حزم نظيفة.

## ما الذي يُرفع على الـVPS؟
1. **apps/cloud-laravel** فقط (باك اند + واجهة الويب).
2. إعداد PostgreSQL + Nginx + PHP-FPM كما في [LARAVEL_SAAS_SETUP](./LARAVEL_SAAS_SETUP.md).

## ما الذي يعمل محليًا في موقع العميل؟
- **apps/edge-server** مع البيئة والمحركات والـAI models. يعمل حتى بدون إنترنت مع فترة سماح 14 يومًا بعد انتهاء الترخيص.
- الوسائط (صور/فيديو) تُخزَّن محليًا فقط؛ المزامنة للسحاب ترسل الميتاداتا.

## أين الواجهة والتطبيق؟
- **apps/mobile-app**: تشغيل `flutter pub get` ثم `flutter run` أو `flutter build apk` بعد `cd apps/mobile-app`.

## روابط الأدلة السريعة
- تثبيت الـCloud/Edge: [docs/CLOUD_EDGE_INSTALLATION.md](./CLOUD_EDGE_INSTALLATION.md)
- دليل التكامل السحابي والـAPI: [docs/CLOUD_INTEGRATION_GUIDE.md](./CLOUD_INTEGRATION_GUIDE.md)
- تحميل وتجهيز الحزم: [docs/DOWNLOAD.md](./DOWNLOAD.md)

> ملاحظة: استخدم `git archive` أو السكربت `scripts/package.sh` للحصول على حزم خفيفة بدون ملفات غير ضرورية (مثل node_modules أو مخلفات البناء).
