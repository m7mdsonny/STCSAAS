# دليل مجلد `apps`

يوضح هذا الدليل وظيفة كل مجلد داخل `apps/` وكيفية استخدامه أثناء النشر أو التطوير. البوابة الويب مدمجة بالكامل ضمن حزمة Laravel السحابية ولا يوجد مجلد مستقل لها.

> تم تنظيف الشجرة من أي مجلدات قديمة (مثل `web-portal/` أو `cloud-server/`) للإبقاء على ثلاث حزم فقط: السحابة Laravel، الخادم المحلي، وتطبيق الجوال.

## 🟦 `apps/cloud-laravel`
- **الوظيفة:** الحزمة السحابية الكاملة (Back-end + Web Portal) المبنية على Laravel 11 مع Sanctum. جميع واجهات الويب والـAPI في نفس المشروع لتبسيط النشر بنمط SaaS.
- **الاستضافة:** VPS (Nginx + PHP-FPM) مع قاعدة بيانات PostgreSQL الجاهزة في `database/schema.sql` وبيانات تجريبية.
- **المكونات المهمة:**
  - `app/Http/Controllers/` مسارات المصادقة، التراخيص، المزامنة مع الـEdge، والإشعارات.
  - `routes/api.php` و`routes/web.php` لتجميع REST API والبوابة المدمجة.
  - `.env.example` قوالب الإعداد (قاعدة البيانات، البريد، Firebase، مفاتيح التراخيص).
  - `composer.json` و`package.json` لتثبيت الاعتمادات PHP/JS.

## 🟧 `apps/edge-server`
- **الوظيفة:** خادم الموقع المحلي (FastAPI + خدمات الـAI + التكاملات) يعمل بدون إنترنت مع تخزين محلي ومزامنة لاحقة.
- **النشر:** يُبنى كملف تنفيذي عبر `scripts/build_installer.sh` (PyInstaller) أو يُشغّل كخدمة systemd على Linux.
- **المكونات المهمة:**
  - `app/api/` نقاط REST (الإعداد، الأحداث، المزامنة، الترخيص).
  - `app/core/` قواعد البيانات المحلية، إدارة الترخيص بفترة السماح، الضبط العام.
  - `app/services/` خدمة المزامنة والمهام الخلفية.
  - `config/` إعدادات البيئة الافتراضية والقوالب.
  - `scripts/` أدوات البناء والتعبئة.

## 🟩 `apps/mobile-app`
- **الوظيفة:** تطبيق Flutter (Android/iOS) يستهلك Laravel API لإدارة الحسابات، التنبيهات، الأحداث، والـLive View.
- **المكونات المهمة:**
  - `lib/` الشاشات، المزودات، ونماذج البيانات.
  - `pubspec.yaml` الاعتمادات وتكوين Firebase/Push.
  - مجلدات المنصات `android/` و`ios/` لبناء الحزم.

## 🔗 طريقة العمل مع الحزم
- **التزامن:** الـEdge يرسل ميتاداتا الأحداث وترخيصه إلى Laravel API عبر REST، بينما يحتفظ بالوسائط محليًا.
- **الترخيص:** الـEdge يتحقق من صلاحية الترخيص عبر Laravel API مع فترة سماح 14 يومًا عند انقطاع الإنترنت.
- **التطبيق الجوال:** يتصل بنفس Laravel API ويستقبل الإشعارات عبر Firebase/FCM.

## 🚀 أين أبدأ؟
- للنشر السحابي: ابدأ بـ `apps/cloud-laravel` (انقل `.env.example` إلى `.env` ثم `composer install`).
- للخادم المحلي: استخدم `apps/edge-server/scripts/build_installer.sh` أو شغل الخدمة مباشرةً من الكود.
- للموبايل: افتح `apps/mobile-app` وشغّل `flutter pub get` ثم `flutter run`.
