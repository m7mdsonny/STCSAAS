# 📥 تنزيل الحزمة الجاهزة للمشروع

يوضح هذا الدليل كيفية إنشاء ملف ZIP نظيف من الشفرة الحالية بدون الملفات الضخمة أو المولدة تلقائيًا مثل `node_modules` أو المخرجات البنائية.

## المتطلبات المسبقة
- تثبيت **Git** ومتوفّر في الـPATH.
- تنفيذ الأوامر من جذر المستودع (`/workspace/STC-Solutions`).

## إنشاء حزمة ZIP
شغّل السكربت المرفق وسيُنشئ نسخة قابلة للمشاركة في مجلد `dist/`، مع رابط ثابت للاحدث:

```bash
./scripts/package.sh
```

سيتم إنشاء ملف جديد باسم مثل `dist/stc-solutions-YYYYMMDD-HHMMSS.zip`، مع رابط ثابت إلى أحدث نسخة تحت `dist/latest.zip`.

### تحديد فرع أو مرجع معيّن
يمكنك تمرير مرجع Git بديل (فرع، تاغ، أو commit) لإنشاء نسخة محددة:

```bash
./scripts/package.sh main
```

## مخرجات السكربت
- يتم حفظ الملف باسم مثل `dist/stc-solutions-YYYYMMDD-HHMMSS.zip`.
- الحزمة تتضمن الملفات المتتبَّعة في Git فقط، مما يضمن حجمًا صغيرًا وسرعة في التحميل.
- إذا أردت تضمين ملفات مولدة (مثل build)، تأكد من تتبعها في Git قبل تشغيل السكربت.

## التحقق
بعد إنشاء الحزمة، يمكنك التأكد من سلامتها بفك الضغط أو عرض محتوياتها:

```bash
unzip -l dist/stc-solutions-*.zip | head
```

سترى قائمة بالملفات الرئيسة للمشروع جاهزة للنقل أو الرفع إلى أي مزود استضافة.
